# playbooks/templates/longhorn-dashboard-auth-middleware.yaml.j2
# ✅ Versión corregida y segura
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: longhorn-dashboard-auth
  namespace: {{ namespace }}
  labels:
    app.kubernetes.io/name: longhorn
    app.kubernetes.io/component: middleware
    access: internal
    environment: prod
  annotations:
    maintained-by: devops@socialdevs.site
    created-by: ansible
spec:
  basicAuth:
    secret: "{{ secret_name }}"
    removeHeader: true  # 🔐 Seguridad adicional
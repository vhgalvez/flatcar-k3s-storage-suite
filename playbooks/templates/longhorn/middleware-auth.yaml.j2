# playbooks\templates\longhorn-dashboard-auth-middleware.yaml.j2
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: longhorn-dashboard-auth
  namespace: {{ namespace }}
spec:
  basicAuth:
    secret: {{ secret_name }}